import React from "react";

// this component will need to conditionally rendered inside the home component

const Spirits = (/*props to be passed in represent strains*/) => {
    // this will be where we iterate over the strains
    return (
        <section>
            <h2>Spirits</h2>
        </section>
    )
}

export default Spirits;